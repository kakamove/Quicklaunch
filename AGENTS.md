# Project Instructions & Agent Guidelines

## OpenSpec 语言规范 (OpenSpec Language Policy)

在为此项目生成或更新 OpenSpec 变更文档（包含 `proposal.md`, `design.md`, `tasks.md`, `specs/**/*.spec.md` 等）时：

1. **业务与逻辑描述使用中文**：所有的意图背景、痛点分析、设计方案推导、逻辑说明、任务清单及场景描述统一使用 **简体中文** 编写。
2. **保留 Schema 结构标题与关键字**：OpenSpec 的结构标题与断言关键字（如 `## Intent`, `## Capabilities`, `Scenario:`, `GIVEN`, `WHEN`, `THEN`, `SHALL`, `MUST` 等）保留英文。
3. **保留技术术语与代码符号**：专有名词、技术术语、CLI 命令、文件名、代码符号（例如 `tmux`, `Ghostty`, `zsh`, `config.yaml`, `fzf`, `xcworkspace` 等）保持英文原文。
